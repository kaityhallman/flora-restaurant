<h1>entrées</h1>
<div class="row">
  <% @menus.each do |menu| %>
    <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
    <h2><%= menu.entree %></h2>
    <div class="caption">
    <h3><%= menu.description %></h3>
    <h5>$<%= menu.price %></h5>
    </div>
    </div>
    </div>
    <% end %>
    </div>
<center>
<%= link_to 'suggest a new entrée', new_menu_path, class: "btn btn-primary" %>
</center>

<!-- 


<table>
  <thead>
    <tr>
      <th>entrée</th>
      <th>description</th>
      <th>price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @menus.each do |menu| %>
      <tr>
        <td><%= menu.entree %></td>
        <td><%= menu.description %></td>
        <td><%= menu.price %></td>
        <td><%= link_to 'Show', menu %></td>
        <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
        <td><%= link_to 'Destroy', menu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'suggest a new entrée', new_menu_path %>
 -->